# Copy this file to .env (or export the variables directly)
# before running the revision export CLI.

# Absolute path to the OAuth client secrets JSON downloaded from
# Google Cloud Console (OAuth 2.0 Client IDs for Desktop app).
GOOGLE_OAUTH_CLIENT_SECRETS=/absolute/path/to/client_secret.json
